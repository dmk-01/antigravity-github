
  # Implement Routing Structure

  This is a code bundle for Implement Routing Structure. The original project is available at https://www.figma.com/design/ylLTvLeTZnbOVPbdtaaPl2/Implement-Routing-Structure.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  